import { TrayHeaderDirective } from './tray-header-directive';

describe('TrayHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TrayHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
