import { TrayContentDirective } from './tray-content-directive';

describe('TrayContentDirective', () => {
  it('should create an instance', () => {
    const directive = new TrayContentDirective();
    expect(directive).toBeTruthy();
  });
});
